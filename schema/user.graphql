type User {
    id: ID!
    name: String!
    created_at: Time!
    updated_at: Time!
}

input CreateUserInput {
    name: String!
}

extend type Query {
    allUsers: [User]!
    user(id: ID!): User!
}

extend type Mutation {
    createUser(input: CreateUserInput): User!
}
